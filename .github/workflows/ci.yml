name: Build and Push Docker Image

on:
  push:
    branches:
      - main
      - master

jobs:
  build-and-push:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/cat-dog-classifier:latest

      - name: Pull latest image
        run: docker pull ${{ secrets.DOCKERHUB_USERNAME }}/cat-dog-classifier:latest

      - name: Stop existing container
        run: |
          docker stop catdog-container || true
          docker rm catdog-container || true

      - name: Run new container
        run: |
          docker run -d -p 8000:8000 --name catdog-container --restart always ${{ secrets.DOCKERHUB_USERNAME }}/cat-dog-classifier:latest